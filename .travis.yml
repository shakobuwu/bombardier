sudo: required
dist: trusty
language: c

services:
  - docker

install:
  - make build

script:
  - test $(docker run --rm -it waja/bombardier --help | head -1 | cut -d' ' -f2 | xargs basename) = "bombardier"
